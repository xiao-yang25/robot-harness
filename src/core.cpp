namespace robot_harness {

// M0 only proves that the standalone C++17 Core target can be built and
// linked. Product types and API enter in M1 after the state model is tested.
int core_build_anchor() noexcept {
  return 0;
}

}  // namespace robot_harness
